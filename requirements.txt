# ── 核心 ─────────────────────────────────────────────────────────────────────
streamlit>=1.40.0
pandas>=2.2.0
pillow>=10.0.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
anthropic>=0.40.0

# ── v3 新增：Supabase ─────────────────────────────────────────────────────────
supabase>=2.4.0

# ── 可選：Google 整合（Drive 照片上傳 + Sheets 同步）────────────────────────
# 不裝也能跑（會自動降級為本機模式）
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-httplib2>=0.2.0
